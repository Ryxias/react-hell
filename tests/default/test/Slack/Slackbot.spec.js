// 'use strict';
//
//
// // Setup only Slackbots
// const { ServiceContainer, FactoryLoader } = require('service-container');
// require('express-route-registry').useContainer(require('service-container'));
//
// const service_container = new ServiceContainer();
//
// const factory_loader = new FactoryLoader(service_container);
//
// factory_loader.load(require('./config/slackbots'));
//
// this.service_container.get('chuubot').connect().then('Connection to chuubot established.');
// this.service_container.get('nicobot').connect().then('Connection to nicobot established.');
//
//
